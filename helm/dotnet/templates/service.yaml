apiVersion: v1
kind: Service
metadata:
  name: {{ include "dotnet.name" . }}
spec:
  type: ClusterIP
  ports:
    - port: 81
      targetPort: 81
      protocol: TCP
  selector:
    {{ include "dotnet.selectorLabels" . | nindent 4 }}
